{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB: Debug single gtest",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${workspaceFolder}/eelib/build/test_eelib",
      "args": ["--gtest_filter=${input:testFilter}"],
      "cwd": "${workspaceFolder}/eelib/build",
      "stopOnEntry": false
    }
  ],
  "inputs": [
    {
      "id": "testFilter",
      "type": "promptString",
      "description": "GTest filter (e.g. MatcherTest.AddSellLimit_PopulatesAsk)"
    }
  ]
}
